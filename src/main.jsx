import React from 'react';
import ReactDOM from 'react-dom/client';
import CartContainer from './components/card-item/CartContainer';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CartContainer /> {}
  </React.StrictMode>
);
